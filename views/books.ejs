<!DOCTYPE html>
<html lang="en">
<head>
   
    <% include ./partials/head %>
</head>
<body>
    <% include ./partials/navbar %>
     <div class="container">
        <div class="row">
            <div class="col s12 m6">
            <% if(books.length){ %>
            <% for(var i=books.length-1;i>=0;i--) { %>
                <div class="card ">
                        <div class="card-content text-center">
                        <div class="col s2 d-flex justify-content-between align-items-center">
                            <label>
                                <input type="checkbox" class="filled-in"  />

                            </label>
                        </div>
                        <div class="col s8 d-flex justify-content-between align-items-center">
                           <span><h3><%= books[i].title %></h3></span><br>
                           <span><h4><%= books[i].author %></h4></span><br>
                           <% if(books[i].city){ %>
                           <span><h4><%= books[i].ownersname %> from <%= books[i].city %></h4></span><br>
                            <% }else{ %>
                           <span><h4><%= books[i].ownersname %> </h4></span><br>
                            <% } %>
                        </div>
                        <div class="col s2 d-flex justify-content-between align-items-center">
                            <label>
                             <%  if(books[i].ownersname==user.username){ %>
                                <i id="<%= books[i]._id %>" class="material-icons">cancel</i>
                            <% } %>
                            </label>
                        </div>
                        </div>

                       
                </div>
                <% } %>
               <% } %> 
            </div>
       </div>
    </div>
</body>
</html>